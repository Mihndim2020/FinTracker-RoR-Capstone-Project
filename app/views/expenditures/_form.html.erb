<%= form_with(model: expenditure) do |form| %>
  <% if expenditure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expenditure.errors.count, "error") %> prohibited this expenditure from being saved:</h2>

      <ul>
        <% expenditure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <div class="field">
    <%= form.label :name,style:"display:none" %>
    <%= form.text_field :name,placeholder:'Name your Expense', class:'name-input' %>
</div>

<div class="field">
    <%= form.label :amount, style:"display:none" %>
    <%= form.number_field :amount, placeholder:'Amount Spent', class: 'name-input' %>
</div>

<div class="field">
    <%= form.label :group_id, class: 'input-group-text' %>
    <%= form.collection_select(:group_id, Group.all, :id, :name, {include_blank: true}, {class: 'group-select w-100'}) %>
</div>

<div class="actions">
    <%= form.submit class:"btn btn-outline-primary text-dark"%>
</div>
<% end %>
