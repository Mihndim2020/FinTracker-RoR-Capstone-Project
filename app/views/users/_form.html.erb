<div class = "new-user-form d-flex flex-column align-items-center">
  <%= form_with(model: user) do |form| %>
    <% if user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
          <% user.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field" >
      <%= form.text_field :name, placeholder:'Name', class: 'name-input' %>
    </div>

    <div class="field">
      <%= form.text_field :email, placeholder:'Email', class: 'name-input' %>
    </div>

    <div class="actions d-flex flex-column align-items-center my-3">
      <%= form.submit class:'btn btn-outline-primary new-user-btn text-dark' %>
    </div>
  </div>
<% end %>
